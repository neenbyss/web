import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Blog",
  description: "Artículos y novedades de Neenbyss Studios.",
}

export default async function BlogPage() {

  return (
    <main className="container-screen-2xl py-30">
  
    </main>
  )
}
