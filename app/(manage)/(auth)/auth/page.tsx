// TODO(auth): pantalla de acceso pendiente de implementar.
// Placeholder para que la ruta sea un módulo válido y el build no rompa.
export const metadata = {
  title: "Acceso",
}

export default function AuthPage() {
  return null
}
